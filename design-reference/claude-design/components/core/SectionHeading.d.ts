export interface SectionHeadingProps {
  eyebrow?: string;
  /** Pass a <span style={{fontStyle:'italic'}}> around the emphasized word for the brand's italic-emphasis pattern. */
  title?: React.ReactNode;
  align?: 'left' | 'center';
}
