Text field with optional label, used in the consultation/contact form.

```jsx
<Input label="Email" placeholder="you@example.com" />
<Input label="Tell us about your space" textarea />
```

Hairline border, sharp-ish radius (`--radius-s`) — the one place the brand uses visible borders. Border darkens on focus.
